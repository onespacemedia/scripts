#!/usr/bin/env bash

virtualenv --no-site-packages venv --distribute
echo "venv" >> .gitignore
source venv/bin/activate
pip install psycopg2 django-suit django-optimizations django-reversion django-usertools django-historylinks django-watson django-south
